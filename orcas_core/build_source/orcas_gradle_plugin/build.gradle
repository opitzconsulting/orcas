apply plugin: 'groovy'

apply from: '../maven_publish.gradle'
project.ext.maven_artifactId = 'orcas-gradle-plugin'
project.ext.maven_description = 'orcas gradle plugin'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile project(':orcas_diff')
}

sourceSets {
    main {
        resources {
            srcDir "../../sql_util_scripts"
        }
    }
}

